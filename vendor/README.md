# Vendor dependencies

This folder contains static JS modules copied from `node_modules` for GitHub Pages hosting.
The import map in `index.html` points here.

See `LICENSES.md` for package versions and licenses.
